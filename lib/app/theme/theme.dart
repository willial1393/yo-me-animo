import 'package:flutter/material.dart';
import 'package:movies_app/app/theme/colors.dart';

final theme = ThemeData.light().copyWith(
  colorScheme: ColorScheme.fromSeed(seedColor: EColor.primary),
);
